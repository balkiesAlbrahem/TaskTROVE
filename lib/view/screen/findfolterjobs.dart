import 'package:flutter/material.dart';

class Findfolterjobs extends StatelessWidget {
  const Findfolterjobs({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("filter"),
      ),
    );
  }
}
